# Состояние production — 09.09.2026

Опубликован утверждённый вариант с локального порта 4175. Основной сайт — [трибека.рф](https://xn--80abmkm6an.xn--p1ai/), REG.RU. Релиз `0e7c2522a6ac5bbed35f5c0ee2305ccbeec198c8-34283079543-1`, исходный UI `f4d7a1c`; последующие изменения до merge затрагивали только CI и документацию.

- [Релизный PR #4](https://github.com/sstarovoitovv/tribeka/pull/4) объединён в main.
- [Production deployment — success](https://github.com/sstarovoitovv/tribeka/actions/runs/34283079543).
- [Quality — success](https://github.com/sstarovoitovv/tribeka/actions/runs/34283079635).

## Проверено после публикации

Тексты main/footer на главной, контактах, услугах, странице компании и политике совпадают с утверждённой локальной сборкой. Сохранены Manrope, исходная фотография с плавным переходом, цветные иконки, компактный подвал, карта, заглушки и нижняя мобильная навигация. Chrome на 390/1440 px не обнаружил горизонтального переполнения или ошибок JS. Предупреждение о согласии видно в форме; браузер не отправляет заявку без галочки.

PHP OPTIONS возвращает 204 и X-Tribeka-Release нового релиза. Прямой POST без согласия возвращает 422. Служебные пути, дампы, вложения, исходники конфигураций и удалённый `/api/analytics.php` закрыты 403/404. Никаких событий аналитики даже со старым opt-in в браузере. Production-таблицы после проверки содержат прежние 0 заявок и 0 вложений; успешные тестовые заявки и реальные письма не создавались.

## Резервные копии и обслуживание

Перед переключением выполнены отдельный backup и автоматический backup деплоя. SHA-256 файлов копии проверены. Атомарный релиз активен, предыдущий релиз сохранён для rollback.

Удалены cron очистки аналитики и устаревшие настройки analytics_dir/analytics_campaigns; предыдущий config и crontab сохранены вне web root с правами 600. Остались ежедневная ротация backup (02:15), backup (02:30) и очистка просроченных заявок (03:23), по времени сервера. Аналитический сбор отсутствует; технические журналы хостинга и встроенная карта не менялись. Внешнее хранение резервных копий и подтверждение кабинетов поисковиков всё ещё ожидают настройки владельцем.

## История: релиз 07.09.2026

### Предыдущая публикация

Основной сайт: https://xn--80abmkm6an.xn--p1ai (трибека.рф), REG.RU. Vercel служит для просмотра; разработка продолжается в develop. Релиз: `0554a9ac5e3c76bf467c3d5c54da60a0e27617a3-34139998685-1`.

- [Production deployment — success](https://github.com/sstarovoitovv/tribeka/actions/runs/34139998685).
- [Quality — success](https://github.com/sstarovoitovv/tribeka/actions/runs/34139998646): lint, frontend, PHP, backup/deploy, SSG, Apache, Vercel routing и e2e.
- PR #1 объединён в develop; PR #2 — в main.

## Подтверждено на основном домене

Главная, about, contacts и страница услуги возвращают 200. Несуществующий URL и закрытая PHP-библиотека возвращают 404. HTTPS www перенаправляет 301 на apex, сохраняя путь и параметры. Проверены CSP, HSTS, nosniff, frame/referrer/permissions headers. OPTIONS формы возвращает 204 и актуальный X-Tribeka-Release. Содержимое about видно без JavaScript.

Chrome подтвердил фактически отрисованный Manrope-ExtraBold, opacity страницы 1 и видимую нижнюю мобильную навигацию. Ошибок JavaScript при просмотре главной не обнаружено. Сохранены палитра, карта и заглушки. Убрана анимация прозрачности, из-за которой предыдущие снимки выглядели тускло. Кнопки и иконки остаются текстом/SVG. Для более чёткого растрового логотипа нужен исходный SVG или экспорт PNG достаточного размера; увеличение маленького исходника деталей не добавляет.

## Сервер и данные

Docroot ссылается на `tribeka-app/current/public`, current — на отдельный каталог релиза. Исходный физический каталог сохранён как `xn--80abmkm6an.xn--p1ai.pre-atomic-20260907`; bootstrap и предыдущий код также сохранены. Старые bootstrap/physical копии содержат временное ограничение POST периода миграции: при ручном возврате к ним отдельно проверить доступность формы. Обычный релиз переключается через атомарную замену current с HTTP-проверкой и возвратом при ошибке.

Созданы две закрытые копии данных, в том числе автоматическая перед production-переключением. SHA-256 файлов первой копии проверены, SQL восстановлен в отдельные временные таблицы той же БД: оба набора содержали 0 записей. Проверена вставка со связью lead/attachment в транзакции тестовых таблиц, затем откат и удаление тестовых таблиц. Права хостинга не позволяют создать отдельную БД; production-таблицы не изменялись. Реальные уведомления не отправлялись. Проверка пустой базы не заменяет будущую проверку восстановления непустых вложений.

Установлен ежедневный cron по времени сервера:

- 02:00 — очистка старых агрегатов аналитики;
- 02:15 — независимая ротация backup по возрасту;
- 02:30 — резервная копия SQL, вложений, конфигурации и существующих агрегатов;
- 03:23 — очистка просроченных заявок.

Команды очистки и ротации выполнены вручную с кодом 0; backup успешно выполнялся перед релизом. Журнал: `data/logs/tribeka-maintenance.log`. Хранятся до 10 копий, не старше 14 дней. Копии находятся на том же сервере; внешнее хранилище пока не подключено.

## Оставшиеся зависимости от владельца

Sitemap опубликован и указан в robots.txt. Отправка через Google Search Console и Яндекс Вебмастер ещё не выполнена: нужны аккаунты владельца и подтверждение домена. Пароли хостинга не заменяют эти аккаунты. Порядок — в [search-and-analytics.md](search-and-analytics.md).

Именованные URL услуг ждут утверждённых названий; механизм slug/aliases и 301 готов. Заглушки исключены из sitemap. Аналитика работает после отдельного разрешения посетителя, UTM campaign пока группируются как other до утверждения списка кодов кампаний. Закрытый отчёт доступен через `current/server/analytics-report.php`.
